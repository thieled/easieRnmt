% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_preprocessing.R
\name{detect_languages_fasttext}
\alias{detect_languages_fasttext}
\title{Detect Languages with fastText}
\usage{
detect_languages_fasttext(
  x,
  text_col = "text",
  id_col = NULL,
  model_path = NULL,
  prob_threshold = 0.25,
  und_label = "und",
  threads = parallel::detectCores(),
  verbose = TRUE
)
}
\arguments{
\item{x}{Character vector or data.frame containing texts to identify.}

\item{text_col}{Character, name of the text column if \code{x} is a data.frame. Ignored if \code{x} is a vector.}

\item{id_col}{Character, optional column in data.frame with unique ids. If NULL, sequential row ids are used.}

\item{model_path}{Character, path to the fastText lid.176.bin model. Defaults to ~/.cache/easieRnmt/lid.176.bin.}

\item{prob_threshold}{Numeric, threshold below which the detected language is replaced by "und". Default = 0.25.}

\item{und_label}{Character, label for undefined language. Default = "und".}

\item{threads}{Integer, number of threads for fastText. Default = parallel::detectCores().}

\item{verbose}{Logical, whether to print progress messages.}
}
\value{
A data.table with columns: \code{row_id}, \code{text}, \code{lang}, \code{lang_prob},
and optionally the user-provided id column.
}
\description{
Identifies the language of each text in a vector or data.frame using fastText's
pretrained language identification model. Cleans problematic inputs (line breaks, control chars, invalid bytes).
Returns a data.table with row ids, input text, detected language, and confidence.
}
